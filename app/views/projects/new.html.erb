<h3 class="text-center pt-5">新規プロジェクト</h3>
<%= form_with model: @project, local: true do |f| %>

  <div class="form-group pt-5 text-center row">
    <%= f.label :name, "プロジェクト名", class: 'col-md-3 offset-md-3 col-form-label' %>
    <%= f.text_field :name, placeholder: "未来について", class: 'form-control w-25' %>
  </div>

  <div class="form-group pt-3 text-center row">
    <%= f.label :introduction, "詳細", class: 'col-md-3 offset-md-3 col-form-label' %>
    <%= f.text_area :introduction, placeholder: "年金制度について考える", class: 'form-control w-50' %>
  </div>

  <div class="form-group pt-3 text-center row">
    <%= f.label :project_image, "グループ画像", class: 'col-md-3 offset-md-3 col-form-label' %>
    <%= f.attachment_field :project_image, class: "form-control-file w-50" %>
  </div>

  <div class="form-group pt-5 text-center row">
    <%= f.submit "始動", class: 'btn btn-success form-control w-25 offset-md-6' %>
  </div>
<% end %>
